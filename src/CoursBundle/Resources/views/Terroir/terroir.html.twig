{# empty Twig template #}
<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title> Les terroir
           s </title>
    </head>
        
    <body>
	<a href="{{ path('add_terroir') }}">Ajouter un terroir
       </a> 
	
        <h4> Les terroir
           s </h4>
        <table>
            <thead>
                <tr>
                    <th>Nom</th>
                    <th>Détail</th>
                    <th>Suppression</th>
                </tr>
            </thead> 
            <tbody>
                {% for terroir in terroirs %}
                    <tr>
                        <td> {{ terroir.nom }}  </td>
                        
                        <td> <a href= "{{ path('info_terroir',{ 'id' : terroir.id }) }}" > Détails </a> </td>
                        <td> <a href= "{{ path('suppression_terroir',{ 'id' : terroir.id }) }}" > suppression </a> </td>
                    </tr>    
                {% endfor %}
            </tbody> 
        </table>
            <a href= "{{ path('cours_homepage') }}" > Retour à la liste </a>  
    </body>
</html>
