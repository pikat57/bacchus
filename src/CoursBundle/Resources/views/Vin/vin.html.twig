{# empty Twig template #}
<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title> Les vins
           s </title>
    </head>
        
    <body>
	<a href="{{ path('add_vin') }}">Ajouter un vin
       </a> 
	
        <h4> Les vins </h4>
        <table>
            <thead>
                <tr>
                    <th>Nom</th>
                    <th>Détail</th>
                    <th>Suppression</th>
                </tr>
            </thead> 
            <tbody>
                {% for vin in vins %}
                    <tr>
                        <td> {{ vin.nom }}  </td>
                        <td> {{ vin.millesime }}  </td>
                        
                        <td> <a href= "{{ path('info_vin',{ 'id' : vin.id }) }}" > Détails </a> </td>
                        <td> <a href= "{{ path('suppression_vin',{ 'id' : vin.id }) }}" > suppression </a> </td>
                    </tr>    
                {% endfor %}
            </tbody> 
        </table>
            <a href= "{{ path('cours_homepage') }}" > Retour à la liste </a>  
    </body>
</html>
